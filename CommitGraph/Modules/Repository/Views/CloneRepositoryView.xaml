<UserControl x:Class="CommitGraph.Modules.Repository.Views.CloneRepositoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:CommitGraph.Modules.Repository.ViewModels"
             MinWidth="320"
             d:DataContext="{d:DesignInstance viewModels:CloneRepositoryViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <StackPanel Margin="5">
        <Label>Remote URL</Label>
        <TextBox Text="{Binding RemoteUrl}" />
        <Label>Working Directory</Label>
        <TextBox Text="{Binding WorkingDirectory}" />
        <Button Margin="0,5,0,0"
                Command="{Binding CloneCommand}"
                Content="Clone" />
    </StackPanel>
</UserControl>